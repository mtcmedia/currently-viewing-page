<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Expiry
    |--------------------------------------------------------------------------
    |
    | Amount of seconds it will take for user to be considered expired and
    | removed from viewing the page unless
    |
    */

    'watch_expiry_seconds' => 100,

    /*
    |--------------------------------------------------------------------------
    | Cache prefix
    |--------------------------------------------------------------------------
    |
    | Configure prefix used for storing the data
    |
    */

    'prefix' => 'currently-viewing-page',
];